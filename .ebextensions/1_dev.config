container_commands:
  01_init_db:
    command: if [ "$ENVIRONMENT" = "dev" ]; then source /var/app/venv/staging-LQM1lest/bin/activate && ./scripts/seed_rds.sh && python -m scripts.seed-db; else exit 0; fi;
    leader_only: true
